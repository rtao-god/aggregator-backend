name: Reconcile backend solution

on:
  push:
    branches: [main]
    paths:
      - '.tools/complete-backend.py'
      - '.github/workflows/reconcile-solution.yml'
      - 'src/**/*.csproj'
      - 'tests/**/*.csproj'
      - 'AggregatorBackend.slnx'
  workflow_dispatch:

permissions:
  contents: write
  statuses: write

concurrency:
  group: reconcile-backend-solution
  cancel-in-progress: false

env:
  DOTNET_CLI_TELEMETRY_OPTOUT: '1'
  DOTNET_NOLOGO: '1'
  NUGET_XMLDOC_MODE: skip

jobs:
  reconcile-build-test:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.302

      - name: Reconcile physical project inventory
        run: python3 .tools/complete-backend.py

      - name: Restore canonical solution
        run: dotnet restore AggregatorBackend.slnx

      - name: Apply canonical formatting
        run: dotnet format AggregatorBackend.slnx --no-restore

      - name: Build every included project
        run: dotnet build AggregatorBackend.slnx --no-restore -warnaserror

      - name: Test every included test project
        run: dotnet test AggregatorBackend.slnx --no-build --logger "console;verbosity=normal"

      - name: Verify deterministic reconciliation
        run: python3 .tools/complete-backend.py --check

      - name: Commit canonical inventory
        id: commit
        shell: bash
        run: |
          if git diff --quiet --exit-code; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Reconcile physical backend project inventory [completion-bot]"
          git push origin HEAD:main
          echo "changed=true" >> "$GITHUB_OUTPUT"

      - name: Publish connector-readable status
        if: always()
        uses: actions/github-script@v9
        env:
          JOB_STATUS: ${{ job.status }}
        with:
          script: |
            const state = process.env.JOB_STATUS === 'success' ? 'success' : 'failure';
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state,
              target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
              description: `Solution reconciliation ${context.runId}: ${process.env.JOB_STATUS}`,
              context: 'ci/reconcile-solution'
            });
