diff --git a/tests/Catalog/Catalog.Application.Tests/CatalogEndToEndTests.cs b/tests/Catalog/Catalog.Application.Tests/CatalogEndToEndTests.cs
index 4a1c2406936095f7cb86817f2739b4d98e578e9d..24300303026fff98078eadf055c65634b975ebbb 100644
--- a/tests/Catalog/Catalog.Application.Tests/CatalogEndToEndTests.cs
+++ b/tests/Catalog/Catalog.Application.Tests/CatalogEndToEndTests.cs
@@ -1,4 +1,5 @@
 using System.Security.Cryptography;
+using System.Text;
 using Aggregator.Catalog.Application;
 using Aggregator.Catalog.Contracts;
 using Aggregator.Catalog.Domain;
@@ -17,7 +18,7 @@ public sealed class CatalogEndToEndTests
     [Fact]
     public async Task CatalogFlowImportsConfigurationPublishesAndRollsBackExactRevisions()
     {
-        var identifiers = Enumerable.Range(10, 10)
+        var identifiers = Enumerable.Range(10, 20)
             .Select(number => Guid.Parse($"0192f5f0-0000-7000-8000-{number:D12}"))
             .ToArray();
         var repository = new InMemoryCatalogRepository();
@@ -111,9 +112,17 @@ public sealed class CatalogEndToEndTests
             actor,
             CancellationToken.None);
 
+        Assert.Equal(identifiers[1], firstRevision.Id);
         Assert.Equal(firstPublication.Id, rollback.Id);
         Assert.Equal(firstPublication.Id, repository.CurrentPublicationId);
         Assert.Equal(2, artifactStore.Artifacts.Count);
+        var firstArtifactJson = Encoding.UTF8.GetString(
+            artifactStore.Artifacts[firstPublication.ArtifactKey]);
+        Assert.Contains("\"contractRevision\":3", firstArtifactJson, StringComparison.Ordinal);
+        Assert.Contains(
+            $"\"contactId\":\"{identifiers[2]:D}\"",
+            firstArtifactJson,
+            StringComparison.Ordinal);
         Assert.Equal(3, repository.OutboxMessages.Count);
         Assert.Equal(4, repository.NextPublicationActivationRevision);
         Assert.All(
