# Physical project inventory

This compact file is generated by `.tools/complete-backend.py`. The solution files are the canonical, exhaustive project lists; this document records their counts without duplicating every path.

- Complete projects: **100**
- Runtime projects: **60**
- Test projects: **40**

## Ownership

- `AggregatorBackend.slnx` is the only complete solution owner.
- `AggregatorBackend.Runtime.slnx` contains every project under `src/` and no test project.
- Physical `.csproj` discovery and `ProjectReference` validation are performed directly from the repository tree.

## Enforcement

CI runs the reconciler in `--check` mode before any complete-solution restore. It fails on orphan projects, broken references, stale solution files, or stale inventory counts.
