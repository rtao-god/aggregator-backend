# Connector capability

The repository write connector can create files directly on the existing `main` branch. This record is removed or folded into the final acceptance documentation after the implementation gate closes.
